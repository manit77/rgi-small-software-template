# Small Web App Template

A requirements management system with AI is intended to help users capture software requirements with an AI-assisted workflow.

## **Project Vision**

RGI is a Requirements Gathering System with the use of AI to assist users in capturing software requirements to generate software\. Users organize requirements as a component tree\. Components can contain many documents\. A Project can be exported so other AI agents can later consume them to generate software\.  This system allows for the structured organization and consumption of knowledge across different AI models\.

### **Primary goal**

1.  Capture high\-quality project requirements in a structured, editable markup format\.
2. Documents can be organized on Tree Heiarchy
3. Preserve enough context so downstream AI agents can reliably implement software from the exported requirements\.
4. Use an AI bot to assist with document writing\.
5. Have sophisticated document editing features\.
6. Full project can be exported to JSON, PDF, Github\.

![AI Requirements Gathering Interface](http://localhost:6060/files/generated-images/image-mnmk9v5r.jpg)

###

### Global Objects

The Global Objects represent the core entities within the system, defining the structure and relationships necessary for capturing and organizing software requirements\. From user authentication to the hierarchical Project Tree, these objects facilitate a seamless workflow for requirement management and AI\-driven software generation\.

- User \- authenticated user in the system
- Project \- a project created to store a tree of components and documents
- Project Tree \-  parent child relationship of components
- Component \- can store multiple documents
- Document \- a mark up text document
- User Settings \- settings applied for the user, can be shared with other users, and the value can be marked as secret\.
- App Settings \- application wide settings accessed by the admins only

Example Project Tree:

```markdown
RGI (project)
├── Overview (component)
│   ├── Project Vision (document)
│   └── Global Objects (document)
├── UI (group)
│   ├── Core UI (component)
│   │   ├── Theme (document)
│   │   └── UIElements (document)
└── Page Views
│   ├── Guest (component)
│   │   ├── LoginPage (document)
│   ├── Authenticated (component)
│   │   └── Dashboard (document)
│── Database (component)
│   └── DatabaseSchema (component)
│── Business Rules (component)
│   └── Business Rules (document)
```

## Development Guidelines & Tech Stack

**\[System Role\]** You are an expert Senior Full\-Stack TypeScript Developer and AI Integration Specialist\. Your objective is to write, review, and refactor code according to the strict architectural guidelines and standards outlined below\.

### **Core Technology Stack**

- **Runtime:** Node\.js
- **Language:** TypeScript \(Strict mode enabled\. NEVER use `any`\. Prioritize interfaces and Zod\-inferred types\)\.
- **Database:** PostgreSQL
  
    - All field names are snake case\.
    - All primary keys are named id and stored as a uuid7 generated by the server
    - each table will contain, user\_createdby, user\_modifiedby, date\_created, date\_modified
- **ORM/Data Access:** ZenStack \(`zenstackhq/orm`\)\.
- **Frontend:** React, TypeScript, Vite
- **Project Structure:** mono repo, each package will have these scripts: build, start\. always include: README\.md, \.env\.example
- **Git:** always include a \.gitignore

### **Required Libraries & Tooling**

- `zod`: For all schema definitions and runtime validation\.
- `swagger-ui-express` & `@asteasolutions/zod-to-openapi`: For maintaining a single source of truth\. Zod schemas MUST drive both runtime validation and the generated OpenAPI specifications\.
- `jsonwebtoken`: For secure, stateless authentication and service\-to\-service AI communication\.
- `pino` or `winston`: For structured logging\.
- `vitest`: For frontend and backend unit/integration testing\.

### **Implementation Rules**

**1\. Data Access & Security**

- **Schema\-First:** Always define models and access control policies in the ZenStack `.zmodel` file\. Do not bypass ZenStack for raw database queries unless absolutely necessary\.
- **Authentication:** Protect all API routes using JWTs\. Enforce zero\-trust principles\.
- **Secrets:** Never hardcode API keys\. Always use environment variables \(`process.env`\) and validate them at application startup using Zod\.

**2\. AI Integration & Operations**

- **Resilience:** All outbound AI API calls \(OpenAI, Anthropic, etc\.\) MUST implement exponential backoff and retry logic to handle `429` and `500` errors gracefully\.
- **Observability:** Log token usage, latency, and context\-length limits for all AI generations\.
- **Testing:** When writing tests, ALWAYS mock AI external service calls to prevent unpredictable outputs and API costs during CI/CD\.

**3\. API Documentation**

- Every new API endpoint must have a corresponding Zod schema for its request body, parameters, and response\.
- Register all schemas using `zod-to-openapi` to automatically update the Swagger UI\. Do not write manual Swagger comments\.

### **Response Directives**

- When generating code, always provide fully typed TypeScript\.
- Include standard error handling \(`try/catch` blocks\) and structured logging in all functional logic\.
- If a request violates these architectural guidelines, alert the user and propose the compliant alternative\.

### **UI Components**

1. components \- reusable components
2. pages \- each page view that holds it's own route\.
   
     1. ie: /login, /logout, /dashboard, /projectDashboard
3. confirmations  and modals \- do not use the browsers alert

## Core UI

### Theme

1. Dark Theme
   
     1. Primary: \#100439
     2. Secondary: \#1E293B
     3. Warning: \#F59E0B
     4. Backgroud: \#000614
     5. Text: \#737373
2. Light Theme
   
     1. Primary: \#2563EB
     2. Secondary: \#F1F5F9
     3. Warning: \#EAB308
     4. Background;
     5. Text:

### UI Elements

**Buttons**

All buttons should have an icon\.

1. large buttons must have text
2. small buttons just an icon

**Grids**

1. columns are sortable
2. pagination enabled at the bottom of the grid
3. last column as action buttons: edit, delete

**Confirmation Modal**

1. will have custom Title
2. body will have a confirmation message
3. Yes, No

**Modals**

1. Will have a custom Title
2. body can contain injected components that can be wired to the Ok or Cancel buttons\.
3. buttons can have custom labels

### Guest Page Views

single\-page UI with a footer, main component is centered\.

### Login Page

A secure, single\-page interface for user authentication, featuring fields for username and password, along with options for account recovery and multi\-factor authentication \(MFA\) support\.

- User login form field: username and password
- new user registration is available if the site config permits, links to new user registration page
- password recovery links to password recovery page

### Password Recovery

To reset your password, please follow these instructions: click the 'Forgot Password' link on the login screen and enter your registered email address\. The user will receive a secure link via email to create a new password and regain access to your account\.   
  
The link will expire based on the siteConfig\.  
  
When the user clicks on the link,  they can set a new password and get back into your account\.

### User Registration

The user registration process is designed to securely onboard new members into the platform if configured in the App Settings\.

**Registration Fields**

- Username \- Unique identifier for login
- Email Address \- Primary contact and verification
- Password \- Secure credential \(must meet complexity requirements\)
- Display Name \- Legal or display name of the user

## Authenticated Page Views

1. Top Nav \- Title / Dashboard / Settings  / Users / Theme / Avatar / Logout
2. Component Loader \- This is the standard viewport area where components are loaded
3. AI Chat  Panel \- allow AI chat bot to interact with the document

```
+------------------------------------------------------------------+
|                            TOP NAV                               |
+------------------------------------------------------------------+
|  MAIN COMPONENT LOADER             | AI CHAT PANEL               |
|      (Left Panel)                  | (Right Panel)               |
|         (75%)                      |      (25%)                  |
+------------------------------------------------------------------+
```

### Dashboard

Summary: This dashboard provides a centralized view of all active initiatives, enabling quick navigation and status monitoring across the platform\.

**Project Search**

\[ Enter project name or ID to filter\.\.\. \]

**List of Projects**

- Project Alpha \- Active
- Project Beta \- In Review
- Project Gamma \- On Hold
- Project Delta \- Draft
- Project Delta \- Cancelled

### Project Dashboard

1. left pane \- Project Tree, include project name and version, a toggle to show Documents in the tree
2. right pane \- control loader for ProjectEdit, Component Document Tab

```
+--------------+--------------------------------------------------+
|  LEFT PANE       |                 MAIN CONTENT                 |
|  (Width: 25%)    |                 (Width: 75%)                 |
|                  |                                              |
|  - Project Tree  |              [Component loader]              |
|  - Filters       |                                              |
|  - Settings      |                                              |
|                  |                                              |
+------------------|                                              |
| Add Component    |                                              |
+-----------------------------------------------------------------+
```

**Components**

1. Project Tree \- tree ui to show a the project hierarchy
2. Project Edit \- component to edit the project properties like Title and Description
3. Component edit \- component to edit the like Name and Description
4. Document Editor \- HTML Document editor used for editing markup\.

### Settings

1. **User Settings \- **settings bound to the user, this can be shared with everyone in the system
2. App Settings \- accessed by admins only,  modify App wide settings

### Users

1. Users Grid \- columns: username, display name, email, role, action
2. when clicking on delete always provide a confirmation, admin role only, cannot delete self or the admin account\.

### My Profile

Users can modify their own profile and change their password\.

## Database Schema

### Users

| Field | Datatype |
| --- | --- |
| id | text |
| username | text |
| email | text |
| password\_hash | text |
| display\_name | text |

## Projects

| Field | Datatype |
| --- | --- |
| id | text |
| title | text |
| description | text |
| status | text |
| version | text |
| priority | text |
| is\_template | bool |

## Business Rules

### Exporting

Here are the rules for export to PDF or Markup\.

1. Project title and description is injected the in export \- H1 — \# Project Title in markup, <h1\> in PDF
2. Tree sorted by position — all child nodes and documents are sorted by position at every level
3. Parent\-child relationships respected — recursive tree walk follows the hierarchy
4. Component \-  don't inject headings or description
5. Document content is rendered as\-is without injecting the document name